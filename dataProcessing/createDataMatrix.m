function out = createDataMatrix(animIDs,varargin)
    % out = createDataMatrix(animIDs,varargin)
    % creates a matrix in tiddy data format  containing the power spectrum for
    % each winSize second window of data
